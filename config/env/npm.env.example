# DEPRECATED — do not store secrets in npm.env.
# Use config/env/npm.config.example for non-secret defaults.
# Store NPM_TOKEN in Infisical (/homelab/npm) or macOS Keychain.
# See docs/runbooks/nginx-proxy-manager.md
