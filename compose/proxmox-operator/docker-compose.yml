services:
  homelab-operator:
    image: python:3.12-slim
    restart: unless-stopped
    command: ["sleep", "infinity"]
    working_dir: /workspace
    env_file:
      - /run/homelab-control/operator-homelab.env
    volumes:
      - ../../:/workspace:ro
