# Identity issuance runbook — `agent:finance`

Generated by `python -m apps._shared.identity plan --principal agent:finance`.

- Manifest: `/home/kenns/git/homelab-control/config/agents/agent-finance.yaml`
- Needs operator action: **no**

## Component summary

| Component | Would-status | Summary |
|---|---|---|
| `ssh_key` | ✓ `issued` | SSH key already present at /home/kenns/.ssh/homelab-agents/agent-finance (would skip) |
| `sandbox_image` | ✓ `issued` | image tag agent-finance:latest — already built and confirmed (would skip) |
| `forgejo_account` | ✓ `issued` | already confirmed via `identity confirm` (would skip) |
| `forgejo_pat` | ✓ `issued` | already confirmed via `identity confirm` (would skip) |
| `discord_bot` | ✓ `issued` | already confirmed via `identity confirm` (would skip) |
| `infisical_token` | ✓ `issued` | already confirmed via `identity confirm` (would skip) |

## Operator checklists

### `ssh_key`  (issued)

- Public key contents: `cat /home/kenns/.ssh/homelab-agents/agent-finance.pub` — paste into Forgejo user agent-finance's SSH keys panel if not already there.

### `sandbox_image`  (issued)

- Rebuild on this host: `python3 -m apps._shared.sandbox build --principal agent:finance`
- Verify: `podman images | grep ^localhost/agent-finance`

### `forgejo_account`  (issued)

- To re-issue: `python -m apps._shared.identity revoke --principal agent:finance --component forgejo_account` then re-run `identity issue`.

### `forgejo_pat`  (issued)

- To re-issue: `python -m apps._shared.identity revoke --principal agent:finance --component forgejo_pat` then re-run `identity issue`.

### `discord_bot`  (issued)

- To re-issue: `python -m apps._shared.identity revoke --principal agent:finance --component discord_bot` then re-run `identity issue`.

### `infisical_token`  (issued)

- To re-issue: `python -m apps._shared.identity revoke --principal agent:finance --component infisical_token` then re-run `identity issue`.

---

This runbook is generated from the agent manifest and the local host's available tooling. Re-run the command to refresh after any change to `agent-finance.yaml` or after ssh-keygen / podman become available.
