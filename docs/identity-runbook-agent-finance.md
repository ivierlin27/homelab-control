# Identity issuance runbook — `agent:finance`

Generated by `python -m apps._shared.identity plan --principal agent:finance`.

- Manifest: `/Users/kevin.enns/projects/homelab-control/config/agents/agent-finance.yaml`
- Needs operator action: **yes**

## Component summary

| Component | Would-status | Summary |
|---|---|---|
| `ssh_key` | · `pending` | would generate ed25519 keypair for agent-finance |
| `sandbox_image` | ! `operator_todo` | image tag agent-finance:latest; Containerfile present |
| `forgejo_account` | ! `operator_todo` | Create the bot Forgejo user |
| `forgejo_pat` | ! `operator_todo` | Generate scoped Personal Access Token for the bot |
| `discord_bot` | ! `operator_todo` | Create Discord application + bot, invite, store token |
| `infisical_token` | ! `operator_todo` | Provision Infisical project + scoped access token |

## Operator checklists

### `ssh_key`  (pending)

- Would generate ed25519 keypair at: /Users/kevin.enns/.ssh/homelab-agents/agent-finance (and .pub)
- Equivalent command (the issuer runs this for real on `identity issue`): `ssh-keygen -t ed25519 -N '' -C 'agent-finance@agent:finance' -f /Users/kevin.enns/.ssh/homelab-agents/agent-finance`
- After generation, add the public key to Forgejo user agent-finance (Settings → SSH Keys).

### `sandbox_image`  (operator_todo)

- On every runner host, build the image: `python3 -m apps._shared.sandbox build --principal agent:finance`
- Verify: `podman images | grep ^localhost/agent-finance` and `python3 -m apps._shared.sandbox build --principal agent:finance --print-only`

### `forgejo_account`  (operator_todo)

- As a Forgejo admin, sign in to https://forgejo.dev-path.org and create a user:
-   username: agent-finance
-   email:    agent-finance@forgejo.dev-path.org
-   full name: Finance Steward
-   role: User (NOT admin)
-   password: long random; rotate immediately and discard the value
- Disable web sign-in for the bot user once the PAT is created.
- After creation, run: identity confirm --principal agent:finance --component forgejo_account

### `forgejo_pat`  (operator_todo)

- Sign in to Forgejo as agent-finance (one-time) at https://forgejo.dev-path.org
- Generate a Personal Access Token with the minimum scopes for the agent's role:
-   scope: read
- Copy the token, then immediately store it in Infisical under the agent's secrets_profile as FORGEJO_TOKEN.
- Sign out and disable web sign-in for the bot user.
- After storing, run: identity confirm --principal agent:finance --component forgejo_pat

### `discord_bot`  (operator_todo)

- Visit https://discord.com/developers/applications and create an app named: agent-finance
- Under 'Bot': add a bot, copy the token (this is the only time it's shown).
- Under 'Bot' > 'Privileged Gateway Intents': enable Message Content Intent only if needed.
- Generate an OAuth2 invite URL with these scopes/perms only:
-   scopes: bot, applications.commands
-   permissions: Send Messages, Read Message History, Create Public Threads, Send Messages in Threads, Add Reactions, Embed Links
- Invite the bot to Kevin's guild.
- Add the bot to these channels: #finance
- Store the token in Infisical under the agent's secrets_profile as DISCORD_BOT_TOKEN.
- After storing, run: identity confirm --principal agent:finance --component discord_bot

### `infisical_token`  (operator_todo)

- Sign in to Infisical as admin at https://infisical.dev-path.org
- Create a project / environment scope named: finance
- Grant the bot identity an access token scoped to that project, read-only by default.
- Save the access token to ~/.config/homelab-control/agent-finance.env on the runner host as INFISICAL_TOKEN.
- After storing, run: identity confirm --principal agent:finance --component infisical_token

---

This runbook is generated from the agent manifest and the local host's available tooling. Re-run the command to refresh after any change to `agent-finance.yaml` or after ssh-keygen / podman become available.
