# Identity issuance runbook — `agent:executive`

Generated by `python -m apps._shared.identity plan --principal agent:executive`.

- Manifest: `/home/kenns/git/homelab-control/config/agents/agent-executive.yaml`
- Needs operator action: **yes**

## Component summary

| Component | Would-status | Summary |
|---|---|---|
| `ssh_key` | ✓ `issued` | SSH key already present at /home/kenns/.ssh/homelab-agents/agent-executive (would skip) |
| `sandbox_image` | ! `operator_todo` | image tag agent-executive:latest; Containerfile present |
| `forgejo_account` | ! `operator_todo` | Create the bot Forgejo user |
| `forgejo_pat` | ! `operator_todo` | Generate scoped Personal Access Token for the bot |
| `discord_bot` | ! `operator_todo` | Create Discord application + bot, invite, store token |
| `infisical_token` | ! `operator_todo` | Provision Infisical project + scoped access token |

## Operator checklists

### `ssh_key`  (issued)

- Public key contents: `cat /home/kenns/.ssh/homelab-agents/agent-executive.pub` — paste into Forgejo user agent-executive's SSH keys panel if not already there.

### `sandbox_image`  (operator_todo)

- On every runner host, build the image: `python3 -m apps._shared.sandbox build --principal agent:executive`
- Verify: `podman images | grep ^localhost/agent-executive` and `python3 -m apps._shared.sandbox build --principal agent:executive --print-only`

### `forgejo_account`  (operator_todo)

- As a Forgejo admin, sign in to https://forgejo.dev-path.org and create a user:
-   username: agent-executive
-   email:    agent-executive@forgejo.dev-path.org
-   full name: Executive Assistant
-   role: User (NOT admin)
-   password: long random; rotate immediately and discard the value
- Disable web sign-in for the bot user once the PAT is created.
- After creation, run: identity confirm --principal agent:executive --component forgejo_account

### `forgejo_pat`  (operator_todo)

- Sign in to Forgejo as agent-executive (one-time) at https://forgejo.dev-path.org
- Generate a Personal Access Token with the minimum scopes for the agent's role:
-   scope: read
- Copy the token, then immediately store it in Infisical under the agent's secrets_profile as FORGEJO_TOKEN.
- Sign out and disable web sign-in for the bot user.
- After storing, run: identity confirm --principal agent:executive --component forgejo_pat

### `discord_bot`  (operator_todo)

- Visit https://discord.com/developers/applications and create an app named: agent-executive
- Under 'Bot': add a bot, copy the token (this is the only time it's shown).
- Under 'Bot' > 'Privileged Gateway Intents': enable Message Content Intent only if needed.
- Generate an OAuth2 invite URL with these scopes/perms only:
-   scopes: bot, applications.commands
-   permissions: Send Messages, Read Message History, Create Public Threads, Send Messages in Threads, Add Reactions, Embed Links
- Invite the bot to Kevin's guild.
- Add the bot to these channels: #intake, #approvals, #ops, #homelab, #knowledge, #insights, #language, #finance, #profile
- Store the token in Infisical under the agent's secrets_profile as DISCORD_BOT_TOKEN.
- After storing, run: identity confirm --principal agent:executive --component discord_bot

### `infisical_token`  (operator_todo)

- Sign in to Infisical as admin at https://infisical.dev-path.org
- Create a project / environment scope named: executive
- Grant the bot identity an access token scoped to that project, read-only by default.
- Save the access token to ~/.config/homelab-control/agent-executive.env on the runner host as INFISICAL_TOKEN.
- After storing, run: identity confirm --principal agent:executive --component infisical_token

---

This runbook is generated from the agent manifest and the local host's available tooling. Re-run the command to refresh after any change to `agent-executive.yaml` or after ssh-keygen / podman become available.
