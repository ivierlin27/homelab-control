# Identity issuance runbook — `agent:homelab`

Generated by `python -m apps._shared.identity plan --principal agent:homelab`.

- Manifest: `/home/kenns/git/homelab-control/config/agents/agent-homelab.yaml`
- Needs operator action: **no**

## Component summary

| Component | Would-status | Summary |
|---|---|---|
| `ssh_key` | ✓ `issued` | SSH key already present at /home/kenns/.ssh/homelab-agents/agent-homelab (would skip) |
| `sandbox_image` | ✓ `issued` | image tag agent-homelab:latest — already built and confirmed (would skip) |
| `forgejo_account` | ✓ `issued` | already confirmed via `identity confirm` (would skip) |
| `forgejo_pat` | ✓ `issued` | already confirmed via `identity confirm` (would skip) |
| `discord_bot` | ✓ `issued` | already confirmed via `identity confirm` (would skip) |
| `infisical_token` | ✓ `issued` | already confirmed via `identity confirm` (would skip) |

## Operator checklists

### `ssh_key`  (issued)

- Public key contents: `cat /home/kenns/.ssh/homelab-agents/agent-homelab.pub` — paste into Forgejo user agent-homelab's SSH keys panel if not already there.

### `sandbox_image`  (issued)

- Rebuild on this host: `python3 -m apps._shared.sandbox build --principal agent:homelab`
- Verify: `podman images | grep ^localhost/agent-homelab`

### `forgejo_account`  (issued)

- To re-issue: `python -m apps._shared.identity revoke --principal agent:homelab --component forgejo_account` then re-run `identity issue`.

### `forgejo_pat`  (issued)

- To re-issue: `python -m apps._shared.identity revoke --principal agent:homelab --component forgejo_pat` then re-run `identity issue`.

### `discord_bot`  (issued)

- To re-issue: `python -m apps._shared.identity revoke --principal agent:homelab --component discord_bot` then re-run `identity issue`.

### `infisical_token`  (issued)

- To re-issue: `python -m apps._shared.identity revoke --principal agent:homelab --component infisical_token` then re-run `identity issue`.

---

This runbook is generated from the agent manifest and the local host's available tooling. Re-run the command to refresh after any change to `agent-homelab.yaml` or after ssh-keygen / podman become available.
