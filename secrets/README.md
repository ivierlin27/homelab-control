# Machine secrets. Encrypted with age via SOPS (see `.sops.yaml`).
# Decrypt: `scripts/sops-render.sh <name>` → `/run/homelab-control/<name>.env`
#
# Do not put plaintext here. Harvest lives in `secrets/.plain/` (gitignored).
