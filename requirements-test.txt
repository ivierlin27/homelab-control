# Minimum dependency set to run the homelab-control test suite.
# Pinned to what the Alienware .venv actually runs (Python 3.14).
#
# Live deployments use a richer venv (uvicorn, fastapi, discord.py, etc.) —
# this file is intentionally narrow so CI stays fast and reliable.

pytest==9.0.3
pytest-asyncio==1.3.0

# Used by maintenance_scan + audit + dashboard
httpx==0.28.1
anyio==4.13.0
PyYAML==6.0.3

# Dashboard tests pull in starlette test client (FastAPI is its own ladder)
fastapi==0.136.1
starlette==1.0.0
jinja2==3.1.6
# FastAPI form parsing (dashboard maintenance start/end)
python-multipart==0.0.20

# Pydantic used by a handful of agent tests
pydantic==2.13.4
